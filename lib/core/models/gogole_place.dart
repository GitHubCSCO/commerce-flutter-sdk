class GooglePlace {
  String formattedName;
  double latitude;
  double longitude;

  GooglePlace(
      {required this.formattedName,
      required this.latitude,
      required this.longitude});
}
